<style>
    .delete-popup {
        position: fixed;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        justify-content: space-between;
        top: 50%;
        left: 50%;
        height: max-content;
        transform: translate(-50%, -50%);
        width: 800px;
        background-color: whitesmoke;
        box-shadow: 0 0 2px rgba(0, 0, 0, 0.5);
        border-radius: 10px;
        overflow: hidden;
        z-index: 200
    }

        .delete-popup p {
            margin: 20px;
            font-size: 20px;
            line-height: 25px;
            font-weight: 600
        }


    .delete-popup input {
        width: calc(100% - 40px);
        margin: 0 20px 20px 20px;
        font-size: 20px;
        font-weight: 600;
        padding: 10px
    }

        .delete-popup input:focus {
                border-color: red;
                outline: none
            }

    .delete-cnt {
        display: flex;
        width: 100%;
        background-color: lightgray;
        padding: 10px 0;
        align-items: center;
        justify-content: flex-end;
    }

        .delete-cnt .button-left, .delete-cnt .button-right {
            margin: 0 20px 0 0;
            padding: 10px;
            font-weight: 600;
            color: white;
            transition: 0.2s;
            cursor: pointer
        }

        .delete-cnt .button-left {
            background-color: red;
            border: 1px solid red;
        }

        .delete-cnt .button-left:hover {
            background-color: firebrick;
            border: 1px solid firebrick
        }

        .delete-cnt .button-right {
            background-color: slategray;
            border: 1px solid slategray
        }

        .delete-cnt .button-right:hover {
            background-color: dimgray;
            border: 1px solid dimgray
        }

    .delete-popup img {
        position: absolute;
        top: 10px;
        right: 10px;
        height: 32px;
        width: 32px;
        cursor: pointer
    }

    @@media (max-width: 900px) {
        .delete-popup {
            width: 90%
        }
    }
</style>

<div class="delete-popup">
    <p>Бардин А.И. будет удален. Для продолжения введите "Подтверждаю"</p>
    <input />
    <div class="delete-cnt">
        <button class="button-left">Принять</button>
        <button class="button-right">Отмена</button>
    </div>
    <img src="images/close.png" />
</div>

@code {

}
